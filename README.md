This repository lets you quickly set up a pre-commit hook without adding a boilerplate to your project.

## Usage

To install the default pre-commit hook, run:

```sh
nix develop github:akirak/git-hooks
```
